services:
  - type: web
    name: nbm-project-management
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
    rootDir: project-management
